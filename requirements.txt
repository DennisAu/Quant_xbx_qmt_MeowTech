# 实盘无限守护 - 三方库依赖清单
# 版本：3.0
#  ∞MeowTech @萌新小强 @无限进化 @萌新小王 @大蒜
# 创建时间：2025年06月29日 14:30

# ====================================================================
# 核心依赖库
# ====================================================================

# 系统进程管理
psutil>=5.9.0

# 任务调度
schedule>=1.2.0

# GUI界面框架
PyQt5>=5.15.0

# HTTP请求库
requests>=2.28.0

# ====================================================================
# 可选依赖库（用于扩展功能）
# ====================================================================

# 进度条显示（用于QMT版本更新检查）
tqdm>=4.64.0

# ====================================================================
# 内置库（无需安装）
# ====================================================================
# os - 操作系统接口
# sys - 系统特定参数和函数
# json - JSON数据处理
# time - 时间处理
# threading - 多线程支持
# subprocess - 子进程管理
# shutil - 高级文件操作
# socket - 网络编程
# gc - 垃圾回收
# xml.etree.ElementTree - XML解析
# datetime - 日期时间处理
# functools - 函数工具
# weakref - 弱引用
# statistics - 统计函数
# winreg - Windows注册表操作（仅Windows）
# io - 核心I/O工具
# concurrent.futures - 并发执行
# pathlib - 面向对象的文件系统路径
# logging - 日志记录
# ctypes - 调用动态链接库
# urllib.parse - URL解析
# importlib - 导入机制
# re - 正则表达式

# ====================================================================
# 安装说明
# ====================================================================
# 1. 快速安装所有依赖：
#    pip install -r requirements.txt
#
# 2. 逐个安装核心依赖：
#    pip install psutil>=5.9.0
#    pip install schedule>=1.2.0
#    pip install PyQt5>=5.15.0
#    pip install requests>=2.28.0
#
# 3. 可选依赖（按需安装）：
#    pip install tqdm>=4.64.0
#
# ====================================================================
# 注意事项
# ====================================================================
# 1. 本程序主要针对Windows系统设计
# 2. Python版本要求：3.8+
# 3. PyQt5如果安装失败，可尝试使用清华源：
#    pip install -i https://pypi.tuna.tsinghua.edu.cn/simple PyQt5
# 4. 如遇到权限问题，可使用管理员权限运行命令行
# 5. 建议在虚拟环境中安装依赖，避免版本冲突